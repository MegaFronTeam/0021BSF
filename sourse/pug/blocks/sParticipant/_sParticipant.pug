mixin sParticipant()
	- const data = lan.sParticipant
	if data
		// start sParticipant
		+b.SECTION.sParticipant.section#sParticipant&attributes(attributes)
			.container
				+section-title(data)
				+e.row.row
					each item in data.items
						.col-6.col-md-4.col-lg-3
							+e.item
								.img-wrap-center
									+img(`img/${item[0]}`)
								span!= item[1]
		// end sParticipant